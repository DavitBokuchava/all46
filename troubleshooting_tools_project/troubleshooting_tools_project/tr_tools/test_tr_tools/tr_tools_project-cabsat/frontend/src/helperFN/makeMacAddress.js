export const formatMacAddress = (text) => {
    let txt = ""
    if (text.length === 16) {
        text.split("").forEach((el, index) => {

        })
    }
}

// var regexp = /^(([A-Fa-f0-9]{2}[:]){5}[A-Fa-f0-9]{2}[,]?)+$/i;
// var mac_address = "fa:09:91:d5:e4:5a";
// if(regexp.test(mac_address)) {
//     console.log("Valid: "+ mac_address);
// } else {
//     console.log("Invalid: "+ mac_address);
// }
// deviceFields.macAddress.replace(/[^a-fA-F0-9]/g, "")